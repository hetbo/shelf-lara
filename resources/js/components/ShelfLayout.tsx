/*
import { Layout } from 'antd';
import { FolderTreeComponent } from './FolderTree'; // You will create this next
import { FileListComponent } from './FileList';   // And this one too

const { Sider, Content } = Layout;

export function FileManagerLayout() {
    return (
        <Layout style={{ minHeight: '100vh' }}>
            <Sider width={250} theme="light" style={{ borderRight: '1p solid #f0f0f0' }}>
                <FolderTreeComponent />
            </Sider>
            <Layout>
                <Content style={{ padding: '16px' }}>
                    <FileListComponent />
                </Content>
            </Layout>
        </Layout>
    );
}*/
